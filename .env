# Database (using in-memory SQLite for testing)
DATABASE_URL=sqlite://file::memory:?cache=shared

# Auth
JWT_SECRET=test-jwt-secret-for-multi-tenant-demo

# Server
PORT=8000

# Tenant orchestrator
TENANT_ORCHESTRATOR_URL=http://localhost:8081